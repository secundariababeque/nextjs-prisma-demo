version: "3.3"

services:
  nextjs:
    ports:
      - 3000:3000
    build:
      context: .
      dockerfile: Dockerfile
    # image: nextjs-exapmple
    volumes:
      - ./:/app
      - /app/node_modules
      - /app/.next
    # environment:
    #   DATABASE_URL: